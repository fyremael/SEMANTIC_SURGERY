# Probes

The registered synthetic probes cover activation edits, low-rank operators, geometry drift, spectra, locality, off-target regression, rollback, operator-vs-text transfer, dynamics messages, and proof-state transitions.
