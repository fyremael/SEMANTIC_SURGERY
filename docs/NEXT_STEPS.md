# Next Steps

- Verify CI on the expanded 15-probe suite.
- Run optional local real-model smoke tests for Hugging Face and TransformerLens adapters when local paths are configured.
- Add optional multi-local-model real cross-transfer smoke coverage once multiple local checkpoints are available.
- Keep persistent edits, downloads, optimizer steps, and production deployment out of scope.
